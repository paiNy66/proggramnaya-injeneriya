# Тема 2. Базовые операции языка Python
Отчет по Теме #5 выполнил(а):
- Силантьев Филат Юрьевич
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | +  | + |
| Задание 2 | +  | + |
| Задание 3 | +  | + |
| Задание 4 | +  | + |
| Задание 5 | +  | + |
| Задание 6 | +  |  |
| Задание 7 | +  |  |
| Задание 8 | +  |  |
| Задание 9 | +  |  |
| Задание 10 | + |  |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

## Самостоятельная работа №1
### Ресторан на предприятии ведет учет посещений за неделю при помощи кода работника. У них есть список со всеми посещениями за неделю. Ваша задача почитать: 
• Сколько было выдано чеков
• Сколько разных людей посетило ресторан
• Какой работник посетил ресторан больше всех раз 
Список выданных чеков за неделю
[8734, 2345, 8201, 6621, 9999, 1234, 5678, 8201, 8888, 4321, 3365, 1478, 9865, 5555, 7777, 9998, 1111, 2222, 3333, 4444, 5556, 6666, 5410, 7778, 8889, 4445, 1439, 9604, 8201, 3365, 7502, 3016, 4928, 5837, 8201, 2643, 5017, 9682, 8530, 3250, 7193, 9051, 4506, 1987, 3365, 5410, 7168, 7777, 9865, 5678, 8201, 4445, 3016, 4506, 4506]

ch = [8734, 2345, 8201, 6621, 9999, 1234, 5678, 8201, 8888, 4321, 3365, 1478, 9865, 5555, 7777, 9998, 1111, 2222, 3333, 4444, 5556, 6666, 5410, 7778, 8889, 4445, 1439, 9604, 8201, 3365, 7502, 3016, 4928, 5837, 8201, 2643, 5017, 9682, 8530, 3250, 7193, 9051, 4506, 1987, 3365, 5410, 7168, 7777, 9865, 5678, 8201, 4445, 3016, 4506, 4506]

```
cnt = len(ch)
print(f"1. Всего чеков: {cnt}")

uniq = set(ch)
dif = len(uniq)
print(f"2. Разных людей: {dif}")

vis = {}
for code in ch:
    if code in vis:
        vis[code] += 1
    else:
        vis[code] = 1

most = max(vis, key=vis.get)
num = vis[most]

print(f"3. Работник {most} был чаще всех: {num} раз")```
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_1.jpg)

## Выводы

len(ch): Вычисляется общее количество выданных чеков. Функция len() подсчитывает все элементы в списке, включая повторяющиеся посещения.

set(ch): множество из списка чеков, что автоматически удаляет все дубликаты. Затем функция len() подсчитывает количество уникальных кодов работников.

vis: Создается словарь для подсчета частоты посещений каждого работника. С помощью цикла for перебираются все чеки и увеличивается счетчик для каждого кода. 

## Самостоятельная работа №2

### На физкультуре студенты сдавали бег, у преподавателя физкультуры есть список всех результатов, ему нужно узнать:

Три лучшие результата

Три худшие результата

Все результаты начиная с 10

Ваша задача помочь ему в этом.

Список результатов бега:
[10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2, 35.7, 16.9, 27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]

Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будет указана вся необходимая информация.
```
res = [10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2, 35.7, 16.9, 27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]
sort_res = sorted(res)
best = sort_res[:3]
worst = sort_res[-3:]
from_10 = sort_res[sort_res.index(10.2):]
print(f"Три лучшие: {best}")
print(f"Три худшие: {worst}")
print(f"Все с 10: {from_10}")
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_2.jpg)

## Выводы
sorted(res): Список результатов сортируется по возрастанию времени (самый быстрый результат).
sort_res[:3]: Берутся первые три элемента отсортированного списка, которые представляют три лучших результата
sort_res[-3:]: Берутся последние три элемента отсортированного списка, которые представляют три худших результата

## Самостоятельная работа №3
### Преподаватель по математике придумал странную задачку. У вас есть три списка с элементами, каждый элемент которых – длина стороны треугольника, ваша задача найти площади двух треугольников, составленные из максимальных и минимальных элементов полученных списков. Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будут указаны два этих значения.

Три списка:

one = [12, 25, 3, 48, 71]
two = [5, 18, 40, 62, 98]
three = [4, 21, 37, 56, 84]
```
a = [12, 25, 3, 48, 71]
b = [5, 18, 40, 62, 98]
c = [4, 21, 37, 56, 84]

min_a = min(a)
min_b = min(b)
min_c = min(c)

max_a = max(a)
max_b = max(b)
max_c = max(c)


def plos(x, y, z):
    p = (x + y + z) / 2  #п
    return (p * (p - x) * (p - y) * (p - z)) ** 0.5

pl_min = plos(min_a, min_b, min_c)
pl_max = plos(max_a, max_b, max_c)

print(f"Площадь из минимальных: {pl_min:.2f}")
print(f"Площадь из максимальных: {pl_max:.2f}")
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_3.jpg)

## Выводы
min(a), min(b), min(c): Находятся минимальные значения из каждого из трех списков, которые образуют стороны первого треугольника.
max(a), max(b), max(c): Находятся максимальные значения из каждого из трех списков, которые образуют стороны второго треугольника.
plos(x, y, z): Для вычисления площади треугольника по трем сторонам. Сначала вычисляется полупериметр p = (x + y + z) / 2, затем площадь.
plos(min_a, min_b, min_c): Вычисляется площадь треугольника, составленного из минимальных элементов списков.
plos(max_a, max_b, max_c): Вычисляется площадь треугольника, составленного из максимальных элементов списков.


## Самостоятельная работа №4
№№№ Никто не любит получать плохие оценки, поэтому Борис решил это исправить. Допустим, что все оценки студента за семестр хранятся в одном списке. Ваша задача удалить из этого списка все двойки, а все тройки заменить на четверки.

Списки оценок (проверить работу программы на всех трех вариантах):

[2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]

[4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4]

[5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]

Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будут три обновленных массива.

```
g1 = [2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]
g2 = [4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4]
g3 = [5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]

r1 = [4 if x == 3 else x for x in g1 if x != 2]
r2 = [4 if x == 3 else x for x in g2 if x != 2]
r3 = [4 if x == 3 else x for x in g3 if x != 2]

print(r1)
print(r2)
print(r3)
```

### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_4.jpg)

## Выводы
4 if x == 3 else x: Для каждого элемента списка проверяется, равен ли он 3. Если да - возвращается 4, если нет - возвращается исходное значение элемента
if x != 2: Исключает из результирующего списка все элементы, равные 2

## Самостоятельная работа №5
### Вам предоставлены списки натуральных чисел, из них необходимо сформировать множества. При этом следует соблюдать это правило: если какое-либо число повторяется, то преобразовать его в строку по следующему образцу: например, если число 4 повторяется 3 раза, то в множестве будет следующая запись: само число 4, строка «44», строка «444».

Множества для теста:

list_1 = [1, 1, 3, 3, 1]
list_2 = [5, 5, 5, 5, 5, 5, 5]
list_3 = [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]
```
l1 = [1, 1, 3, 3, 1]
l2 = [5, 5, 5, 5, 5, 5, 5]
l3 = [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]
def make_set(lst):
    res = set()
    for num in set(lst):
        cnt = lst.count(num)
        for i in range(1, cnt + 1):
            if i == 1:
                res.add(num)
            else:
                res.add(str(num) * i)
    return res
s1 = make_set(l1)
s2 = make_set(l2)
s3 = make_set(l3)

print(s1)
print(s2)
print(s3)
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/image.png)
## Выводы
set(lst): Для каждого списка создается множество уникальных чисел, чтобы определить какие числа присутствуют в исходных данных
lst.count(num): Для каждого уникального числа подсчитывается количество его повторений в исходном списке
Цикл for i in range(1, cnt + 1): Для каждого числа генерируется последовательность от 1 до количества его повторений включительно (if i == 1: При первом проходе добавляется само число как числовой тип данных, 
else: При последующих проходах создаются строковые представления числа, повторенного i раз с помощью str(num) * i)

## Лабораторная работа №1
### Друзья предложили вам поиграть в игру “найди отличия и убери повторения (версия для программистов)". Суть игры состоит в том, что на вход программы поступает два множества, а ваша задача вывести все элементы первого, которых нет во втором.
```
a = {'White', 'Black', 'Red', 'Pink'}
b = {'Red', 'Green', 'Blue', 'Red'}

print(a - b)
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ1.jpg)
## Выводы
Создание множеств: Переменные a и b содержат множества строковых элементов. Множества автоматически удаляют дубликаты, как видно в множестве b, где элемент 'Red' указан дважды, но сохраняется только один экземпляр
Операция разности множеств: Используется оператор - между множествами a и b, который возвращает все элементы из множества a, которых нет в множестве b

## Лабораторная работа №2
### Напишите две одинаковые программы, только одна будет использовать set(), а вторая frozenset() и попробуйте к исходному множеству добавить несколько элементов, например, через цикл.
```
a = set('xyzabc')
print(a)
for i in range(1, 4):
    a.add(i)
print(a)
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ2.jpg)

## Выводы
set('xyzabc'): Создается изменяемое множество из символов строки. Метод add() позволяет добавлять новые элементы (числа 1, 2, 3) во время выполнения цикла.
frozenset('xyzabc'): Создается неизменяемое "замороженное" множество. При попытке использовать метод add() возникает ошибка AttributeError, так как frozenset не поддерживает операции добавления элементов.
Цикл for i in range(1, 4): В первой программе успешно добавляет элементы 1, 2, 3 в множество, а во второй программе вызывает ошибку при первой же итерации.

## Лабораторная работа №3
### На вход в программу поступает список (минимальной длиной 2 символа). Напишите программу, которая будет менять первый и последний элемент списка.
```
def swap(lst):
    temp = lst[0]
    lst[0] = lst[-1]
    lst[-1] = temp
    return lst

print(swap([10, 20, 30, 40, 50]))
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ3.jpg)
## Выводы
temp = lst[0]: Во временную переменную temp сохраняется значение первого элемента списка для последующего использования.
lst[0] = lst[-1]: Первому элементу списка присваивается значение последнего элемента (доступ через отрицательный индекс -1).

## Лабораторная работа №4
### На вход в программу поступает список (минимальной длиной 10 символов). Напишите программу, которая выводит элементы с индексами от 2 до 6. В программе необходимо использовать "срез".
```
nums = [15, 47, 23, 89, 156, 342, 678, 92, 38, 451, 823, 17]
print(nums[2:6])
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ4.jpg)
## Выводы
nums[2:6]: Применяется операция среза списка, где: 2 - начальный индекс (включаЯ2) 6 - конечный индекс (не включительно)

## Лабораторная работа №5
### Иван задумался о поиске «бесполезного» числа, полученного из списка. Суть поиска в следующем: он берет произвольный список чисел, находит самое большое из них, а затем делит его на длину списка. Студент пока не придумал, где может пригодиться подобное значение, но ищет у вас помощи в реализации такой функции useless().
```
def useless(lst):
    return max(lst) / len(lst)

print(useless([4, 8, 12, 6, 45]))
print(useless([-15.2, 62, 84.7, 0, -42, 105.3, -58, 35.9, 51, -76.8]))
print(useless([-30.5, 95, 18.3, -67, 88.1, 0, 57, -104.2, 72.6, -12]))
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ5.jpg)
## Выводы
max(lst): Функция находит максимальное значение впереданном списке чисе
len(lst): Функция определяет длину списка (количество элементов)
Операция деления /: Максимальное значение делится на длину списка, что дает искомое "бесполезное" число
Тестирование функции: Функция роверяется на трез] различных списках с целыми и вещественными числами, включая отрицательные значения

## Лабораторная работа №6
### Ребята не могут определиться, каким супергероем они хотят стать. У них есть случайно составленный список супергероев, и вы должны определить, кто из ребот будет каким супергероем. Необходимо использовать разделение списков.
         
``` 
hero es = ['ironman', 'thor', 'hulk']
alex, maxim, dmitry = heroes
print('Алекс -', alex)
print('Максим -', maxim)
print('Дмитрий -', dmitry)
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ6.jpg)
## Выводы
heroes = ['ironman', 'thor', 'hulk']: Создается список из трех элементов - имен супергероев
alex, maxim, dmitry = heroes: присваивается соответствующей переменной в том же порядке

## Лабораторная работа №7
### Вовочка, насмотревшись передачи "Слабое звено" решил написать программу, которая также будет находить самое слабое звено (минимальный элемент) и удалять его, только делать он это хочет не с людьми, а со списком. Помогите Вовочке с реализацией программы.
```
nums = [-18.5, 92, 15.3, -42, 68.7, 0, 37, -105.2, 54.8, -9]
nums.sort()
print('Отсортированный список:\n', nums)
nums.pop(0)
print('Список без наименьшего элемента:\n', nums)
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ7.jpg)
## Выводы
nums.sort(): Метод сортирует исходный список по возрастанию, располагая минимальный элемент на первой позиции 
nums.pop(0): Метод удаляет элемент с индексом 0, который после сортировки является наименьшим значением в списке

## Лабораторная работа №8
### Михаил решил создать большой n-мерный список, для этого он случайным образом создал несколько списков, состоящих минимум из 3, а максимум из 10 элементов и поместил их в один большой список. Он также как и Иван не знает зачем ему это сейчас нужно, но надеется на то, что это пригодится ему в будущем.
```
from random import randint

def make_list():
    a = [randint(1, 100)] * randint(3, 10)
    return a

if __name__ == '__main__':
    res = []
    for i in range(randint(1, 5)):
        res.append(make_list())
    print(res)
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ8.jpg)
## Выводы
randint(1, 100): Генерирует случайное число от 1 до 100, которое будет повторяться в каждом внутреннем списке
[число] * randint(3, 10): Создает список, где одно и то же число повторяется случайное количество раз (от 3 до 10)
Цикл for i in range(randint(1, 5)): Создает от 1 до 5 таких списков и добавляет их в основной список res

## Лабораторная работа №9
### Вы работаете в ресторане и отвечаете за статистику покупок, ваша задача сравнить между собой заказы покупателей, которые указаны в разном порядке. Реализуйте функцию superset(), которая принимает 2 множества.
```
def superset(set_1, set_2):
    if set_1 > set_2:
        print(f"Объект {set_1} является чистым супермножеством")
    elif set_1 == set_2:
        print("Множества равны")
    elif set_1 < set_2:
        print(f"Объект {set_2} является чистым супермножеством")
    else:
        print("Супермножество не обнаружено")

if __name__ == '__main__':
    superset({1, 8, 3, 5}, {3, 5})
    superset({1, 8, 3, 5}, {5, 3, 8, 1})
    superset({3, 5}, {5, 3, 8, 1})
    superset({98, 100}, {3, 5})
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ9.jpg)
## Выводы
set_1 > set_2: Проверяет, является ли первое множество строгим супермножеством второго (содержит все элементы второго и имеет дополнительные элементы)
set_1 == set_2: Проверяет равенство множеств независимо отпорядка элементов
set_1 < set_2: Проверяет,  является ли первое множество строгим подмножеством вторОГО

## Лабораторная работа №10
### Предположим, что вам нужно разобрать стопку бумаг, но нужно начать работу с нижней, "переверните стопку". Вам дан произвольный список. Представьте его в обратном порядке. Программа должна занимать не более двух строк в редакторе кода.
```
lst = [4, 9, 2, 7]
print(lst[::-1])
```
### Результат.
![Меню](https://github.com/paiNy66/proggramnaya-injeneriya/blob/Тема_5/Тема_5_ЛАБ10.jpg)
## Выводы
Код занимает всего 2 строки и использует встроенные возможности Python для обращения списка без необходимости написания циклов или дополнительных функций
